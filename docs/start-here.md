# Start here

last-verified: 0000000 2026-07-05

This repo's status lives here (single owner). CI reconciles the `last-verified`
stamp to the pushed commit on every run, so the scored pipeline is always fresh
(the end-session-ritual pattern — see the README).
